# API keys and secrets — never commit
*API Key*
*api-key*

# Environment variables — never commit credentials
.env
.env.local
.env.production
.env.*.local
.env.oneroster
supabase/functions/.env

# Generated at build time by scripts/build-config.js
app/config.js

# Dependencies
node_modules/

# Vercel CLI
.vercel

# Supabase CLI temp files
supabase/.temp/

# One-off local operational scripts (kept locally, not tracked)
rollover-2026.sql

# Local working files with sensitive data — never commit
Background Check Data.numbers
